{
  "for each account, what product tier are they on?": "PREFIX c: <urn:arango-sparql:concept#> SELECT ?name ?tier WHERE { ?a a c:accounts ; c:account_name ?name ; c:current_product_tier ?tier }",
  "for each account, what documents do we hold and where did they come from?": "PREFIX c: <urn:arango-sparql:concept#> SELECT ?name ?tier ?source ?url WHERE { ?a a c:accounts ; c:account_name ?name ; c:current_product_tier ?tier ; c:account_id ?acct . ?d a c:Document ; c:source ?source ; c:citable_url ?url ; c:account_id ?acct }"
}
