jdbc.url=jdbc:postgresql://postgres:5432/crm
jdbc.user=cdf
jdbc.password=cdf
jdbc.driver=org.postgresql.Driver

# JSONB columns (e.g. accounts.products_contracted) have no native RDF datatype;
# fall back to string literals rather than refusing to start.
ontop.inferDefaultDatatype=true
